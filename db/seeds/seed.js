
const dbData = require("../index")

exports.seed = (knex) => {
  /*
   return knex.migrate
   .rollback()
   .then(() => knex.migrate.latest())
   .then(() => knex("example_table").insert(dbData[data]))
 */
}